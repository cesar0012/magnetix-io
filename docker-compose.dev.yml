# Desarrollo local con SQLite.
# La BD es un archivo local (file:./data/db.sqlite definido en .env).
# No se necesita un servicio Docker: solo `pnpm dev` con DATABASE_URL=file:./data/db.sqlite.
# Este archivo queda vacío intencionalmente.
name: vocero-dev
services: {}
